const a = "hello"
const b = "world"
const c = { d: [true, false] }
// console.log(c.d[0])

// this is the same as:
//means we can assign variables values in key of object
console.log({ a1: a, b1: b, c1: c })